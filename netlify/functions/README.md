Fonctions Netlify pour l'API des investisseurs.

Installation des dépendances pour les fonctions (dans netlify/functions):

cd netlify/functions
npm install

Note: Assurez-vous que DATABASE_URL est défini localement pour que Prisma puisse se connecter (ex: sqlite ou Postgres).
